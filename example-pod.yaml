apiVersion: v1
kind: Pod
metadata:
  name: example-pod
  labels:
    app: example-app
spec:
  containers:
  - name: nginx-container
    image: nginx:1.16.0
    ports:
    - containerPort: 80
  restartPolicy: Always